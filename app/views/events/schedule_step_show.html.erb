<%= form_for(form) do |f| %>
  <% if form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(form.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="inputs">
    <div>
      <label>Date</label> <%= f.text_field :date %>
    </div>
    <div>
      <label>Start</label> <%= f.text_field :start %>
    </div>
    <div>
      <label>End</label> <%= f.text_field :event_end %>
    </div>
    <div>
      <label>Location</label> <%= f.text_field :location %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
